import { RoutesObject } from '../../core/types/routes.object.type'

/**
 *
 *
 * will be here to define all public routes
 * the public routes will be defined in the app using `react-router-dom`,
 *   only if user has not been authenticated and has no access to these routes
 * this the concept of high level routing in the SPA (SinglePageApplications)
 */
export const publicRoutes: RoutesObject = {}
